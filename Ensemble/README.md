# ML-Ensemble
This project took multiple models and was trained to classify clothes and another to classify digits. The models would all make a prediction and whatever the majority decided was the output. This model was trained using Google's Colaboratory.
